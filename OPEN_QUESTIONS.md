# OPEN QUESTIONS

> PRD 0.2 규칙 7에 따라, 사양이 모호해 추측이 필요했던 지점을 기록한다.
> 각 항목은 **가장 보수적인 방향(권한이 좁고 기능이 적은 쪽)으로 임시 구현**되어 있다.

---

## OQ-1. AI 호출 카운터의 저장 위치 (NFR-4)

**상황**: NFR-4는 "AI 호출은 사용자당 일 20회 상한(Firestore 카운터로 강제)"을 요구하지만,
4장 데이터 모델에는 카운터 컬렉션이 정의돼 있지 않다.

**임시 구현**: `users/{uid}/usage/{YYYYMMDD}` 서브컬렉션

```ts
{
  hintCount: number;      // 그날 소비한 AI 힌트 호출 수
  updatedAt: Timestamp;
}
```

- 날짜 키는 **Asia/Seoul 기준**으로 끊는다(학교 하루 단위와 맞추기 위해).
- 보안 규칙: 본인·교사 **읽기만** 허용, 쓰기는 서버 라우트(Admin SDK) 전용.
- 호출 **전에 선점**하고 AI 호출이 실패하면 되돌린다. 동시 요청으로 상한을 넘길 수 없다.

**확인 필요**
- 이 경로를 4장에 정식 스키마로 편입할지, 아니면 별도 `usage/` 최상위 컬렉션으로 옮길지.
- 7.2의 **업로드 건수 상한(1인 1일 20장)** 도 같은 문서에 `uploadCount`로 합칠지 분리할지.
- 교사에게도 같은 상한을 적용할지. (현재는 **교사도 동일하게 적용** — 보수적 선택)

---

## OQ-2. 되묻기·재촬영 응답의 저장 여부 (FR-301)

**상황**: FR-301은 되묻기(`[ASK]`)가 레벨을 소모하지 않는다고만 정한다.
이 응답을 `hints` 서브컬렉션에 남길지, 일일 호출 상한을 소모할지는 미정이다.

**임시 구현**
- `hints`에 **저장하지 않는다** (4.3 스키마의 `level: 1|2|3`에 들어갈 자리가 없음).
- `hintLevelUsed`를 **올리지 않는다**.
- 단, 실제 AI 호출이 발생했으므로 **일일 상한은 소모한다**.

**확인 필요**: 되묻기 내용이 화면 새로고침 후에도 남아야 하는가? 남아야 한다면 저장 위치가 필요하다.

---

## OQ-3. 힌트 대화 기록의 보존 범위 (FR-301a)

**상황**: 재생성(FR-302)으로 폐기된 출력과 규칙 위반 사유를 보관할지 미정.

**임시 구현**: 서버 로그(`console.warn`)에만 남기고 Firestore에는 최종 힌트만 저장한다.
9장의 "교사 주간 샘플 점검"을 하려면 위반 사례가 조회 가능해야 할 수 있다.

---

## OQ-4. 추가된 환경변수

라우트 구현 과정에서 PRD 0.1에 없던 환경변수를 도입했다. 이름 확정 필요.

| 변수 | 기본값 | 용도 |
|---|---|---|
| `HINT_DAILY_LIMIT` | `20` | NFR-4 사용자당 일 AI 호출 상한 |
| `HINT_TIMEOUT_MS` | `20000` | NFR-1 AI 응답 하드 타임아웃 (목표는 8초) |
| `FIREBASE_STORAGE_BUCKET` | `{project_id}.firebasestorage.app` | 사진 판독 시 Admin SDK 기본 버킷 |

---

## OQ-5. PRD와 실제 의존성 버전 불일치

- PRD 0.1은 **Next.js 15**로 적혀 있으나 설치된 것은 **16.3.5**다.
  Next 16에서 `export const runtime = 'nodejs'`는 기본값이며 edge 런타임은 폐기됐다.
  PRD 0.1의 "AI·알림 라우트에 `export const runtime = 'nodejs'`를 명시한다" 항목은 더 이상 필요 없다.
- `firebase-admin` **v14**는 네임스페이스 API(`admin.auth()`)를 제거했다.
  `firebase-admin/app`, `/auth`, `/firestore`, `/storage` 모듈 진입점만 사용해야 한다.

**확인 필요**: PRD 0.1 표를 실제 버전에 맞춰 갱신할 것인지.

---

## OQ-6. 로컬 검증 환경 — 해결됨 (2026-09-13)

`firebase-tools` v15의 에뮬레이터는 **JDK 21 이상**을 요구한다(JRE 8은 거부된다).
JDK 21은 이미 설치돼 있었으나 PATH에 없었다. 사용자 환경변수에 등록해 해결했다.

```
JAVA_HOME = C:\Program Files\Microsoft\jdk-21.0.12.101-hotspot
Path      += %JAVA_HOME%\bin
```

**남은 사항**: 다른 개발 PC에서도 같은 설정이 필요하므로 README에 선행 조건으로 적을 것.

---

## OQ-7. 에뮬레이터 projectId 고정 (2026-09-13)

**상황**: 테스트는 `math-qna-test`로 붙는데 에뮬레이터는 `demo-no-project`로 떠서,
`storage.rules`의 `firestore.get()`이 **항상 null**을 반환하고 있었다
(`Null value error`, storage.rules line 6). 그 결과:

- "학생 A가 B의 비공개 사진에 접근하면 거부된다" 테스트는 **규칙이 판단해서가 아니라
  조회가 실패해서** 통과하고 있었다.
- `visibility == 'public'`이면 허용하는 분기는 **한 번도 검증된 적이 없었다.**
  배포 환경에서는 조회가 성공하므로 로컬과 다르게 동작했을 것이다.

**조치**: 양쪽 모두 `demo-math-qna`로 고정하고(`npm run test:rules`에 `--project` 추가),
공개 사진 읽기가 **허용되는지** 확인하는 카나리아 테스트를 추가했다.
projectId가 다시 어긋나면 이 테스트가 실패한다(되돌려서 실제 실패를 확인함).

**확인 필요**: 실제 Firebase 프로젝트 ID가 정해지면 `.firebaserc`를 추가하고,
테스트용 `demo-` 프로젝트와 분리해 둘 것.
