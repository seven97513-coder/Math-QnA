# OPEN QUESTIONS

> PRD 0.2 규칙 7에 따라, 사양이 모호해 추측이 필요했던 지점을 기록한다.
> 각 항목은 **가장 보수적인 방향(권한이 좁고 기능이 적은 쪽)으로 임시 구현**되어 있다.

---

## OQ-1. AI 호출 카운터의 저장 위치 (NFR-4)

**상황**: NFR-4는 "AI 호출은 사용자당 일 20회 상한(Firestore 카운터로 강제)"을 요구하지만,
4장 데이터 모델에는 카운터 컬렉션이 정의돼 있지 않다.

**임시 구현**: `users/{uid}/usage/{YYYYMMDD}` 서브컬렉션

```ts
{
  hintCount: number;      // 그날 소비한 AI 힌트 호출 수
  updatedAt: Timestamp;
}
```

- 날짜 키는 **Asia/Seoul 기준**으로 끊는다(학교 하루 단위와 맞추기 위해).
- 보안 규칙: 본인·교사 **읽기만** 허용, 쓰기는 서버 라우트(Admin SDK) 전용.
- 호출 **전에 선점**하고 AI 호출이 실패하면 되돌린다. 동시 요청으로 상한을 넘길 수 없다.

**확인 필요**
- 이 경로를 4장에 정식 스키마로 편입할지, 아니면 별도 `usage/` 최상위 컬렉션으로 옮길지.
- 7.2의 **업로드 건수 상한(1인 1일 20장)** 도 같은 문서에 `uploadCount`로 합칠지 분리할지.
- 교사에게도 같은 상한을 적용할지. (현재는 **교사도 동일하게 적용** — 보수적 선택)

---

## OQ-2. 되묻기·재촬영 응답의 저장 여부 (FR-301)

**상황**: FR-301은 되묻기(`[ASK]`)가 레벨을 소모하지 않는다고만 정한다.
이 응답을 `hints` 서브컬렉션에 남길지, 일일 호출 상한을 소모할지는 미정이다.

**임시 구현**
- `hints`에 **저장하지 않는다** (4.3 스키마의 `level: 1|2|3`에 들어갈 자리가 없음).
- `hintLevelUsed`를 **올리지 않는다**.
- 단, 실제 AI 호출이 발생했으므로 **일일 상한은 소모한다**.

**확인 필요**: 되묻기 내용이 화면 새로고침 후에도 남아야 하는가? 남아야 한다면 저장 위치가 필요하다.

---

## OQ-3. 힌트 대화 기록의 보존 범위 (FR-301a)

**상황**: 재생성(FR-302)으로 폐기된 출력과 규칙 위반 사유를 보관할지 미정.

**임시 구현**: 서버 로그(`console.warn`)에만 남기고 Firestore에는 최종 힌트만 저장한다.
9장의 "교사 주간 샘플 점검"을 하려면 위반 사례가 조회 가능해야 할 수 있다.

---

## OQ-4. 추가된 환경변수

라우트 구현 과정에서 PRD 0.1에 없던 환경변수를 도입했다. 이름 확정 필요.

| 변수 | 기본값 | 용도 |
|---|---|---|
| `HINT_DAILY_LIMIT` | `20` | NFR-4 사용자당 일 AI 호출 상한 |
| `HINT_TIMEOUT_MS` | `20000` | NFR-1 AI 응답 하드 타임아웃 (목표는 8초) |
| `FIREBASE_STORAGE_BUCKET` | `{project_id}.firebasestorage.app` | 사진 판독 시 Admin SDK 기본 버킷 |
| `TEACHER_EMAILS` | 없음 | FR-102 최초 교사 계정 허용 목록(쉼표 구분). **미설정 시 부트스트랩 불가** |

---

## OQ-5. PRD와 실제 의존성 버전 불일치

- PRD 0.1은 **Next.js 15**로 적혀 있으나 설치된 것은 **16.3.5**다.
  Next 16에서 `export const runtime = 'nodejs'`는 기본값이며 edge 런타임은 폐기됐다.
  PRD 0.1의 "AI·알림 라우트에 `export const runtime = 'nodejs'`를 명시한다" 항목은 더 이상 필요 없다.
- `firebase-admin` **v14**는 네임스페이스 API(`admin.auth()`)를 제거했다.
  `firebase-admin/app`, `/auth`, `/firestore`, `/storage` 모듈 진입점만 사용해야 한다.

**확인 필요**: PRD 0.1 표를 실제 버전에 맞춰 갱신할 것인지.

---

## OQ-6. 로컬 검증 환경 — 해결됨 (2026-09-13)

`firebase-tools` v15의 에뮬레이터는 **JDK 21 이상**을 요구한다(JRE 8은 거부된다).
JDK 21은 이미 설치돼 있었으나 PATH에 없었다. 사용자 환경변수에 등록해 해결했다.

```
JAVA_HOME = C:\Program Files\Microsoft\jdk-21.0.12.101-hotspot
Path      += %JAVA_HOME%\bin
```

**남은 사항**: 다른 개발 PC에서도 같은 설정이 필요하므로 README에 선행 조건으로 적을 것.

---

## OQ-9. AI 제공자를 Gemini → OpenAI로 변경 (2026-09-13)

PRD 0.1의 기술 스택 표는 **"변경 금지"**이며 AI 칸은 Gemini로 못박혀 있다.
실제로는 OpenAI(GPT)로 전환했으므로 PRD 갱신이 필요하다.

**현재 구현**: `/api/hint`는 `OPENAI_API_KEY`가 있으면 OpenAI, 없고 `GEMINI_API_KEY`만
있으면 Gemini를 쓴다. 프롬프트(`lib/prompts/hint.ts`)와 후처리 검사는 제공자와 무관하다.

| 변수 | 기본값 | 비고 |
|---|---|---|
| `OPENAI_API_KEY` | 없음 | 이 키가 있으면 OpenAI 경로 |
| `OPENAI_MODEL` | `gpt-4o-mini` | 이미지 입력이 되는 모델이어야 한다 |
| `GEMINI_API_KEY` / `GEMINI_MODEL` | 없음 / `gemini-3.5-flash` | 폴백 경로 |

### 모델 파라미터 (2026-09-13 실측)

최신 세대 모델은 파라미터 규격이 다르다. 실제로 키로 확인한 결과:

| 파라미터 | `gpt-4o` | `gpt-5.6-luna` |
|---|---|---|
| `max_tokens` | OK | **400 거부** — `max_completion_tokens`를 쓰라고 응답 |
| `max_completion_tokens` | OK | OK |
| `temperature: 0.4` | OK | **400 거부** — 기본값(1)만 허용 |

→ 라우트는 **`max_completion_tokens`만 보내고, `temperature`는 `OPENAI_TEMPERATURE`가
지정됐을 때만** 보낸다. 이 조합이 두 세대 모두에서 동작한다.
추론 모델은 이 한도 안에서 추론 토큰까지 쓰므로 기본값을 2000으로 잡았다(`OPENAI_MAX_COMPLETION_TOKENS`).

### 모델별 힌트 품질 비교 (같은 문제·같은 프롬프트, 2017 6월모평 나형 30번)

| 모델 | 토큰/회 | 1단계 힌트 | 레벨 구분 |
|---|---|---|---|
| `gpt-4o-mini` | 26,785 | "na-a²>0에서 a의 범위를…" | ✗ 1·2·3단계가 같은 말 |
| `gpt-4o` | 2,028 | "로그 함수의 정의역과 자연수 조건을…" | △ 대체로 구분됨 |
| **`gpt-5.6-luna`** | 3,631 | "이차함수의 최댓값과 꼭짓점 성질을…" | ✓ 개념→조건→첫 식 |

`gpt-4o-mini`는 **이미지 토큰을 13배** 쓴다(단가가 싸도 회당 비용은 비슷하다).
현재 선택은 `gpt-5.6-luna`.

**확인 필요**
1. PRD 0.1 표의 AI 칸과 NFR-4 한도를 OpenAI 기준으로 다시 쓸 것.
2. `gpt-5.6-luna` 단가를 OpenAI 대시보드에서 확인하고 NFR-4 일일 상한(현재 20회)을 재조정할 것.
3. **비용**: 사진을 `detail: 'high'`로 보낸다. 손글씨·수식 판독에는 필요하지만
   이미지 토큰이 크게 늘어난다. 7.2의 예산 알림을 OpenAI 쪽에도 걸어야 한다.
4. **수식 표기**: 프롬프트는 KaTeX 인라인 `$...$`를 지시하는데 `gpt-5.6-luna`는
   `\(...\)`로 출력했다. 상세 화면의 렌더러가 두 표기를 모두 처리하는지 확인할 것.

---

## OQ-8. 첫 교사 계정 부트스트랩 (2026-09-13)

**있었던 일**: `/pending` 화면에 "교사 권한 활성화" 버튼이 있었고, `users/{uid}` 규칙이
`allow update: if isOwner(uid)`였다. 여기에 `role()`의 users 문서 폴백이 더해지면서
**누구든 로그인 후 버튼 한 번으로 교사가 되어 모든 비공개 질문을 읽을 수 있는 상태**였다.
PRD 0.2 절대 규칙 2-1 / NFR-9 위반.

**조치**
- 규칙: `create`는 `role == 'pending'`만, `update`는 교사이거나 **role이 그대로일 때만** 허용.
- 화면: 자가 승격 버튼 제거, 콘솔 안내 문구로 교체.
- 테스트 4건 추가(pending 생성 거부 / 문서 role=student 허용 / 자가 승격 거부 / teacher 생성 거부).

**이후 구현 (2026-09-13)**
- 첫 교사: `POST /api/admin/bootstrap`이 `TEACHER_EMAILS` 허용 목록과 대조해 부여한다(FR-102).
- 학생 승인·차단: `/teacher/users` 화면 + `POST /api/admin/users`.
  역할 변경은 users 문서와 **커스텀 클레임을 함께** 갱신하고 `auditLogs`를 남긴다.
- `role`·`isBlocked`는 보안 규칙에서 클라이언트 쓰기를 전면 차단했다(교사 포함).
  두 값은 Admin SDK로만 바뀌므로 감사 로그가 비는 경로가 없다.

**남은 결정**
1. 승인 시 학년은 3으로 고정돼 있다(OQ 원본 3번). 1·2학년을 받으면 화면에 드롭다운이 필요하다.
2. 커스텀 클레임이 붙은 뒤에도 `role()`의 users 문서 폴백은 남아 있다. 폴백을 없애면
   **질문을 읽을 때마다 붙는 문서 읽기 1건**이 사라지지만(7.2 비용), 승인 직후
   재로그인 전까지 접근이 안 된다. 현재는 폴백을 유지해 재로그인 없이 동작하게 두었다.
3. 차단(`isBlocked`)은 클레임이 아니라 문서로만 판정한다. 즉시 효력을 위해 의도한 것이지만,
   **모든 쓰기에 문서 읽기가 1건 추가된다.**

---

## OQ-7. 에뮬레이터 projectId 고정 (2026-09-13)

**상황**: 테스트는 `math-qna-test`로 붙는데 에뮬레이터는 `demo-no-project`로 떠서,
`storage.rules`의 `firestore.get()`이 **항상 null**을 반환하고 있었다
(`Null value error`, storage.rules line 6). 그 결과:

- "학생 A가 B의 비공개 사진에 접근하면 거부된다" 테스트는 **규칙이 판단해서가 아니라
  조회가 실패해서** 통과하고 있었다.
- `visibility == 'public'`이면 허용하는 분기는 **한 번도 검증된 적이 없었다.**
  배포 환경에서는 조회가 성공하므로 로컬과 다르게 동작했을 것이다.

**조치**: 양쪽 모두 `demo-math-qna`로 고정하고(`npm run test:rules`에 `--project` 추가),
공개 사진 읽기가 **허용되는지** 확인하는 카나리아 테스트를 추가했다.
projectId가 다시 어긋나면 이 테스트가 실패한다(되돌려서 실제 실패를 확인함).

**확인 필요**: 실제 Firebase 프로젝트 ID가 정해지면 `.firebaserc`를 추가하고,
테스트용 `demo-` 프로젝트와 분리해 둘 것.
